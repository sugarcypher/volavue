# Cloudflare Pages handles extensionless URLs natively:
#   - /booking serves booking.html with 200
#   - /booking.html 308-redirects to /booking
# Adding explicit /booking -> /booking.html 301 rules here causes a redirect loop.
# Keep this file present (so Pages keeps applying its default behavior) but empty.
